<Window x:Class="GameWatcher.Gui.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="GameWatcher" Height="520" Width="800">
  <Grid Margin="10">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="*"/>
      <ColumnDefinition Width="*"/>
    </Grid.ColumnDefinitions>
    <StackPanel Orientation="Horizontal" Grid.Row="0" Grid.ColumnSpan="2" Margin="0,0,0,8">
      <TextBlock Text="Window filter:" VerticalAlignment="Center"/>
      <TextBox x:Name="FilterBox" Width="220" Margin="8,0,8,0"/>
      <Button Content="Refresh" Click="Refresh_Click" Margin="0,0,8,0"/>
      <TextBlock Text="FPS:" VerticalAlignment="Center"/>
      <TextBox x:Name="FpsBox" Width="50" Margin="8,0,8,0" Text="20"/>
      <Button x:Name="StartBtn" Content="Start" Click="Start_Click" Margin="0,0,8,0"/>
      <Button x:Name="StopBtn" Content="Stop" Click="Stop_Click" IsEnabled="False"/>
    </StackPanel>

    <ListBox x:Name="WindowsList" Grid.Row="1" Grid.Column="0" Height="140"/>
    <StackPanel Grid.Row="1" Grid.Column="1" Margin="8,0,0,0">
      <TextBlock Text="Current Speaker:"/>
      <ComboBox x:Name="SpeakerBox" IsEditable="True" Margin="0,4,0,0"/>
      <StackPanel Orientation="Horizontal" Margin="0,6,0,0">
        <Button Content="Edit Persona" Click="EditPersona_Click"/>
      </StackPanel>
    </StackPanel>

    <TextBox x:Name="LogBox" Grid.Row="2" Grid.ColumnSpan="2" IsReadOnly="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"/>

    <StackPanel Grid.Row="3" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right">
      <TextBlock Text="Tip: Press Stop to end capture"/>
    </StackPanel>
  </Grid>
</Window>
